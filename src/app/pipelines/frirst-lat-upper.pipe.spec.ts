import { FrirstLatUpperPipe } from './frirst-lat-upper.pipe';

describe('FrirstLatUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new FrirstLatUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
